import SetPasswordClient from './SetPasswordClient';

export const metadata = {
  title: '设定密码 · Set Password',
  robots: { index: false, follow: false },
};

export default function SetPasswordPage() {
  return <SetPasswordClient />;
}
